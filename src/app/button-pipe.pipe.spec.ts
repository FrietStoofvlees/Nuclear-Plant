import { ButtonPipePipe } from './button-pipe.pipe';

describe('ButtonPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ButtonPipePipe();
    expect(pipe).toBeTruthy();
  });
});
